name: georouter_app
description: A new Flutter project for GeoRouter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  provider: ^6.1.1
  maplibre_gl: ^0.26.2
  flutter_map_tile_caching: ^10.1.1
  path_provider: ^2.1.0
  sqflite_common_ffi: ^2.3.0
  intl: ^0.20.3
  shared_preferences: ^2.2.2
  sqflite: ^2.3.3+1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
